# 支持与诊断

## 提交 Issue 前

请先确认使用的是最新 Release，并在设置中记录：

- App 版本和 Arclume Wine Runtime 版本；
- macOS 版本、芯片类型与是否使用 CrossOver；
- 选择的图形后端、是否启用 D3DMetal 4、DXVK、msync 或 Metal HUD；
- 可复现步骤、预期结果和实际结果。

游戏兼容性或启动问题请使用兼容性模板。普通 Bug 请使用 Bug 模板。功能请求请说明要解决的用户问题，而不是只给出实现方案。

## 诊断日志与隐私

“设置 → 关于 → 诊断日志 → 打开”可定位 Arclume 保存的日志。日志可能包含本机路径、macOS 用户名、游戏安装路径、进程名、时间和 Runtime 配置；提交前请删除或替换这些内容。

不要提交以下数据：

- Steam 密码、令牌、SSFN、Cookie 或会话文件；
- 整个 CrossOver Bottle 或 `Games` 容器；
- 未脱敏的崩溃报告、录屏或系统诊断；
- 受版权保护的游戏文件、客户端资源或补丁包。

## 不提供的支持

Arclume 不提供游戏账号、付费内容、反作弊绕过、盗版客户端、CrossOver 许可证或第三方驱动支持。对上游 Wine、CrossOver、Steam 或游戏客户端的问题，我们会在可定位到 Arclume 集成层时协助收集最小复现。
