# 替换 Arclume 应用图标

- 使用用户提供的紫蓝色 A 图标生成完整 macOS AppIcon 尺寸资源，保留原图透明度和构图，不额外叠加玻璃、背景或阴影。
- Debug/Release 改用 AppIcon 资产目录；旧 Logo.icon 设计文件保留但不再编译进应用图标。版本号、Runtime 和用户数据不变。
- 已通过无签名 Debug preflight 和本机签名构建启动；应用包 CFBundleIconName 为 AppIcon，Assets.car 核验完整 16–1024 像素图标尺寸，编译图标已人工查看。
