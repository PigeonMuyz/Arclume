# YY 频道阴影窗口对照实验

- 增加只读阴影目标预览及 30 秒临时隐藏对照；仅匹配 YY 可见频道外侧四边各 30 像素、同进程拥有的禁用透明分层 QTool，零个或多个目标均拒绝。
- 不修改父子关系、浏览器窗口样式、配置或 DLL；测试后向原窗口发送不激活的恢复请求，窗口已关闭／替换时不操作新窗口。
- 仅作为显式人工诊断，不进入正式 Adapter。需要验证隐藏期间的画面、按钮反馈，以及恢复后的窗口状态。
