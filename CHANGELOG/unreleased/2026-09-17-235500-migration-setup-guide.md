# 统一容器的分步设置引导

- 沿用原剑网 3 设置向导的左侧步骤、右侧内容结构，统一容器升级分为环境介绍、自动检查、安全迁移和完成四步。
- 正常界面不枚举注册表、缓存、系统差异，不要求用户逐项处理；检查页只给出可迁移状态与空间概况。详细内容留在迁移报告，异常时可手动导出诊断。
- 自动识别已经卸载的安装器磁盘卷及其旧设备映射，避免因 macOS 重用设备编号误报有效盘符冲突。仍然拦截真实应用数据或有效盘符的未决冲突。
- 迁移显示真实阶段进度，仍保留确认、备份、校验和中断恢复；不会自动执行用户的真实迁移。
