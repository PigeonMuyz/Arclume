# YY DNS 内存补丁诊断原型

- 增加手工启用的独立调试工具，只启动固定 YY 入口并处理其调试子进程。
- 校验指定 gslb.dll 路径、完整 SHA-256 和原始指令，在 DLL 加载事件暂停期间将 DNS 接管入口改为返回既有失败值；不改磁盘 DLL 或公共 Runtime。
- 此为兼容性实验，不随 App 自动应用，不代表登录、语音或长期运行已验证；退出进程恢复原始代码。
