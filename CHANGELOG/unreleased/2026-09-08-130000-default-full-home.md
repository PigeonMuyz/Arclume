# 剑三首页默认使用完整模式

- 将首页、设置与窗口背景的 jx3CompactHome 默认值统一为 false；尚未选择首页样式时使用完整模式。
- 简洁模式仍由设置 → 通用 → 外观 → 剑三首页开启；保留用户已保存的选择，不在每次启动时强制重置。
- 不修改用户偏好文件、游戏配置或 Runtime。布局进一步优化暂处于截图评审阶段。
- 验证：PR 前置检查与 Debug 构建，不运行 XCTest。已选择过简洁模式的安装仍会保留简洁模式。
