# 整理运行时设置布局

- 设置卡片统一占满内容栏宽度，移除运行时 Picker 的重复标题。
- 容器准备与 Steam 安装合并为一张卡片；未创建、待安装、安装中、已安装分别显示对应操作，不再对已就绪容器突出显示准备按钮。
- 自动安装作为主操作，本地安装包收纳到更多菜单；保留错误提示与异步进度。CrossOver 容器同步使用此布局。
- 仅调整设置界面，不改变运行时、容器或游戏文件；验证使用 Debug build / PR preflight，不运行 XCTest。
