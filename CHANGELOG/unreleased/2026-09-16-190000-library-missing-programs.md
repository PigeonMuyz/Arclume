# 普通模式隐藏失效的本地程序卡片

- 普通模式统一校验保存的本地程序路径和关联容器，已删除的剑网 3 启动器及其他程序不再继续显示；快捷方式同时检查实际目标。
- 在列表变更、应用重新激活和前台定时检查时刷新，并关闭已失效程序的详情页。
- 校验只影响显示，不删除持久化记录、封面或安装数据；恢复目录后卡片自动恢复。Steam 已拥有但未安装的游戏不受影响。
- 不变更 Runtime、版本或迁移格式；回滚仅移除此显示校验。手工验证：删除测试程序后切回应用，确认卡片消失；恢复同一路径后确认卡片恢复。
