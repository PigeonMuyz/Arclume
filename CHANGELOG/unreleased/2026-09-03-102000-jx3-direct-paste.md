# 剑网 3 输入桥接回退

- 试验过 Wine macOS 编辑菜单的直接消息模式；剑网 3 的自定义输入控件会因此停止接收普通键盘输入，故恢复为稳定的按键桥接模式。
- 后续剪贴板与中文输入的修复将落在独立 Runtime 的输入实现，不能影响普通英文键盘输入。
- 不迁移、不重置现有 Games 容器；设置会在下一次以内置 Wine 启动时写入。
- 调试构建中的显式输入诊断仅记录 Wine 的 IME 与剪贴板通道，不启用会持续膨胀的 server/SEH 全量跟踪。
