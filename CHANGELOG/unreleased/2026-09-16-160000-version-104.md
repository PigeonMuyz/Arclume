# 版本显示调整为 1.0.4

- Debug / Release 的 MARKETING_VERSION 统一为 1.0.4；内部 CFBundleVersion 递增为 9，继续供诊断和兼容旧更新逻辑使用。
- 设置页和当前版本字符串不再拼接构建号；后续发布 tag 和产物使用 v1.0.4，不再使用 v1.0.4-9。每次后续公开发布需递增 MARKETING_VERSION。
- 保留旧版带构建号 tag 的解析与比较；本次不创建 tag、不推送、不触发 Release。
- 本机替换 /Applications/Arclume.app，不重置或复制用户容器与配置；Runtime Manifest 和归档不变。回滚需重新安装旧 App，用户数据保留。

## 验证与本机交付

- 核对 `67eadc8`（1.0.3-8 发布提交）：旧更新器支持无构建号 tag，安装校验的 releaseBuild 可为空，DMG 按后缀定位。因此无需先发布带后缀的过渡版本；尚未实测远端在线升级。
- `./script/pr_preflight.sh` 通过，包含无签名 Debug build、Manifest SHA-256、workflow YAML 和差异校验。
- `xcodebuild test -project Arclume.xcodeproj -scheme Arclume -testPlan ArclumeCore -destination 'platform=macOS' -derivedDataPath /tmp/arclume-context-menu-tests -only-testing:ArclumeTests/ApplicationVersionTests -only-testing:ArclumeTests/LibraryProgramLocationTests DEVELOPMENT_TEAM=8JFR5LUFAJ`：7 项通过。
- 单独执行 Debug `xcodebuild build`，使用 ad-hoc 签名，产物不含测试插件；覆盖 `/Applications/Arclume.app` 后 `codesign --verify --deep --strict` 通过，`rsync -acn --delete --itemize-changes` 对比无差异，安装版本为 1.0.4、内部构建号 9。
- 安装的是本机测试版，未公证、未自动启动、未提交或推送，也未触发公开发布。
