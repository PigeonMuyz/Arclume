# YY 独立 Runtime 测试入口

- 诊断启动脚本允许显式指定带专用标记的独立 YY 加载锁测试 Runtime；未设置环境变量时仍使用原有安装版本。
- 图形版本继续读取原安装 Runtime 的选择，避免归档没有部署标记而改变图形变量。
- 不修改 App 的 Runtime 选择、用户注册表或正式 Runtime；关闭测试后，不指定该环境变量即可回到原有启动路径。
- 这是人工诊断入口，不是已验证或已发布的 YY 适配。频道绘制、交互和音频仍需实测。
