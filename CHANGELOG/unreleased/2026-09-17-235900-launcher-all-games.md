# 统一启动器界面与全部游戏入口

- 主界面统一使用宽屏启动器；移除设置里的网格/启动器切换，旧 libraryPresentation 偏好不再决定界面，保留旧值以便源码回退。
- 侧边栏底部增加固定“全部游戏”入口，收起列表后仍可使用，支持 Command-F、搜索、已安装/全部筛选、Escape 关闭。
- 参考用户提供的原生启动台设计，使用居中的大尺寸系统材质悬浮面板、72pt 图标、单行名称和胶囊筛选；移除项目计数与重复安装状态。点击面板外或 Escape 收起。
- 选择项目仅切换展示并关闭面板，不启动、不安装。未安装项目也可查看，原有启动按钮保持禁用。
- 保留侧边栏顺序及现有拖动效果，首次引导更新为唯一的启动器操作说明。
- 不变更容器、Runtime、安装文件和版本号。不推送、不发布。可回退本次 UI 提交而不迁移用户数据。
