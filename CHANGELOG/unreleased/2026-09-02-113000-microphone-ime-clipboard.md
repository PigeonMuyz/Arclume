# 内置 Wine 的麦克风、中文输入与剪贴板修复

- 在从内置 Wine 启动剑网 3 前，由 Arclume 主应用主动请求 macOS 麦克风权限；Wine 的 CoreAudio 预检不再是用户唯一的授权入口。
- 保留中文区域、代码页与字体回退，但不再覆盖 Wine macOS 驱动动态维护的键盘布局，避免与系统拼音等输入法的映射冲突。
- 显式启用 Wine macOS 编辑菜单的按键桥接，使标准的复制、剪切、粘贴和全选快捷键通过系统剪贴板工作。
