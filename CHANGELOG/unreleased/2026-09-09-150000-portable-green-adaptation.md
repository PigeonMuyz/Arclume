# 绿色软件的可撤销去广告适配

- 将 YY 绿色 BAT 中审核过的广告、推广与内置更新屏蔽路径收录为 JSON 规则；不执行原 BAT，不清空账号配置或注册表，不关闭日志。
- 导入及整包更新支持可选绿色适配，现有软件可从右键菜单预览并应用；旧内容移动到同容器恢复目录，支持撤销和重复应用。
- 拒绝运行中操作、越界、符号链接及特殊文件；部分失败保留清单与旧内容，撤销不覆盖已改写的文件。
- 新增隔离核心测试与规则投稿说明。需要手工验证真实 YY 广告屏蔽、更新和登录兼容性；不修改 Runtime 或发布归档。
