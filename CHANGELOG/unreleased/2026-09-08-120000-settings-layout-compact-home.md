# 设置分组与剑三简洁首页

- 应用设置统一为固定尺寸侧栏导航：通用、更新、关于；普通模式另有运行时、游戏库。保留模式切换、运行时安装进度、更新下载源与诊断日志入口。
- 剑三首页默认使用简洁视图，隐藏海报和资讯图片，公告按需展开；首页及通用设置可切回完整视图，选择持久化。简洁视图不创建图片加载组件，默认不请求资讯 Feed。
- 剑三高级设置分为运行时与图形、游戏、自定义启动参数；显卡使用 Picker，导入与恢复移入更多菜单。
- 普通游戏设置将兼容性、输入设备与自定义参数按需展开，保留图形后端专属选项和原有保存/重置操作。设置面板采用中性背景。
- 不变更 Runtime、游戏配置格式、容器与运行生命周期逻辑；不发布、不替换已安装 App。可通过首页视图开关恢复完整首页。
- 验证：PR 前置检查、无签名 Debug 构建、Runtime SHA-256 与 git diff --check 均已通过，未运行 XCTest；尚需实机逐项检查两种模式分组、视图记忆、设置弹窗及运行/更新进度。
