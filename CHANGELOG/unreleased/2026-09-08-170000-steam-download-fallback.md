# Steam 安装包下载失败回退

- 不再只依赖 Fastly 地址；按顺序尝试 Steam 的 Akamai、备用 CDN 和 Fastly HTTPS 地址，最多三次，不循环重试。
- 界面显示当前下载源与自动切换阶段；网络超时、DNS 和 TLS 错误使用中文提示，保留取消和手动安装包入口。
- 每个地址限制 20 秒无响应超时、180 秒总时长；保留 100 MiB 大小上限，并在启动前检查 HTTP 响应、HTTPS 和 PE 文件头。取消不触发换源，磁盘/权限错误不继续下载。
- 本机原 Fastly 地址出现 TLS 连接失败，两个备用地址 HEAD 均返回 200；完整下载验证不启动安装程序或改动用户容器。
- 验证：独立 Swift 下载检查、Debug 构建、PR preflight；不运行 XCTest。
