# 完整与简洁首页布局优化

- 默认完整模式保持不变。简洁首页使用 720 × 480 点内容窗口；恢复完整首页、离开剑三首页后恢复原有 1024 × 750 点尺寸。可调整窗口的开发模式保持手动尺寸，不对初始化页面或普通游戏弹窗应用紧凑尺寸。
- 完整首页海报高度降至约 38%，缩小轮播缩略图、降低紫色背景亮度，固定启动面板合理宽度。
- 两种首页共用按内容高度排版的启动面板，消除空卡片；完整面板补充游戏状态和图形后端。保留首页常用开关，简洁模式并排、完整模式纵向排列，标签与开关分列对齐。
- Metal HUD 中文翻译改为「性能面板」；DLSS FG 改为「DLSS 帧生成（Beta）」，添加英文键及中英文提示，并同步游戏设置中的入口。此开关仍是能力声明，不改变实际游戏设置逻辑。
- 保留工具栏停止按钮、异步启动状态、设置入口和用户持久化偏好；不修改 Runtime 或真实游戏配置，不覆盖已安装应用，不推送或发布。
- 验证：PR 前置检查、无签名 Debug 构建、Runtime SHA-256、翻译 JSON 校验及 git diff --check 已通过；未运行 XCTest。窗口切换、设置弹窗和游戏运行状态尚需实机验证。
