# 重新发布 2.0（13）

- 按维护者要求以当前升级启动验证通过的代码重新发布 2.0，构建号递增至 13；原 2.0 标签更新到本次发布提交。
- 保留历次发布记录，新增 `released/2.0-13.md`；发布流程优先读取按构建号区分的说明。
- 包含前次迁移低内存／系统组件冲突修复、本轮重置／退出修复及诊断日志。Runtime Manifest 和归档仍为 1.1.2，不改变容器迁移行为。
- 验证范围：无签名 Debug 预检，重置、退出、迁移及版本判断核心隔离测试；用户已手工验证 1.0.3（8）两个容器升级迁移后可启动剑网3启动器。Release 归档、两种 DMG 和校验文件由 GitHub Actions 构建验证。
- 重发版本不会触发现有 2.0／2.0.1／2.0.2 的自动升级，发布说明明确要求手动安装。回退 App 不会逆转已完成的容器合并；不宣称所有设备上的 CPU 类型错误均已解决。
