# 精简设置说明与运行环境启动选项

- 逐页精简通用、运行环境、游戏库及重置页的常驻说明，移除过时的 CrossOver 迁移提示；保留状态、错误与重置风险确认。
- 将开关改为“运行环境随 Arclume 启动”。只存在一个有效容器时自动使用它，多个有效且不同的容器才显示预热选择；旧目标别名按真实路径去重。
- 兼容原有启用开关及多容器偏好，不重写用户选择。开关仍默认关闭，未创建的容器不会因显示设置而初始化。
- 不修改 Runtime 归档、用户容器、游戏或迁移流程。回退代码后沿用原有偏好格式。
- 验证：定向 WineWarmupTests、Debug build、pr_preflight。手工检查开关关闭/开启，单容器隐藏列表，多容器选择保留；真实 Wine 游戏启动未自动执行。
