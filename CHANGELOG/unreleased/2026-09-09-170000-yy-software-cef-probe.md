# YY 内嵌浏览器软件渲染对照实验

- 独立诊断工具增加 `--run-software-cef`，保留 DNS 实验，在新启动的固定 YY 9.58 进程树中添加关闭 GPU 和 GPU 合成参数。
- 只在暂停事件中改写 WOW64 启动参数并读回复核，不修改 libcef、公共 Runtime、注册表、其他程序或绿色适配恢复数据；退出测试进程即回滚。
- 这是待人工验证的参数传递实验，不代表所有 libcef 版本兼容，也未成为 App 默认项。需验证频道内容、语音和重启后再决定设置/Adapter 接入。
