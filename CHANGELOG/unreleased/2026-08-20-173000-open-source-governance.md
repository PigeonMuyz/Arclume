# 建立公开协作与审核基线

- 时间：2026-08-20T17:30:00+08:00
- 类型：docs
- 范围：文档、CI、仓库历史
- PR / 提交：待提交

## 改动

- 重写项目 README，并补充公开协作、Runtime、更新和发布入口。
- 新增贡献、Agent、安全、支持和行为规范。
- 建立目录化、追加式变更记录规则。
- 制定 PR 静态预检和 XCTest 分层计划。
- 将旧私有初始化根提交替换为 `导入 Arclume 开源基线`；最终文件树、`main` 和 `v1.0.1-3` 的发布内容保持不变。
- 将公开定位调整为独立、社区驱动的 Arclume；移除不属于 GPLv3 要求的参考项目说明，仅保留源文件中的版权、许可证和适用的附加法律声明。
- 新增贡献、审核与维护文档，明确 Issue、PR、维护者审核和高风险变更的协作方式。
- 明确公开发行保留内置 Runtime、D3DMetal 与兼容性资源；补充组件边界、版权和对应源码位置，不将第三方组件误述为 Arclume GPL 或开源代码。

## 用户影响与兼容性

- 不改变 App 或 Runtime 的运行行为，也不改变已发布版本对应的文件树。
- 后续每个逻辑改动都必须新增一份变更记录。

## 验证

- `bash -n script/pr_preflight.sh`：通过。
- Issue / workflow YAML 与文档本地链接：通过。
- `./script/pr_preflight.sh`：通过（无签名 Debug build、Runtime SHA-256；未运行 XCTest）。

## 后续

- 在独立变更中落地新的 XCTest Test Plan 与核心测试 CI。
