# 准备发布 Arclume 1.0.3（8）

- 类型：release
- 范围：App、图标、发布索引

## 版本与范围

- App 的 Debug/Release 版本统一为 1.0.3，构建号为 8；发布标签为 v1.0.3-8。
- 发布内容与兼容性边界见 [版本索引](../released/1.0.3-8.md)。保留既有追加式记录原位，不重命名或删除历史记录。
- 内置 Runtime Manifest、ABI、LFS 归档均不变；独立实验 Runtime 不打入发布包。
- YY 的频道成功结果来自手动实验组合，不宣称普通启动已默认解决 YY 崩溃／白屏；本次不修改正式 YY 启动规则。

## 验证与回滚

- 发布前运行无签名 Debug preflight、差异检查和 Release 构建；由既有 Actions 制作 with-runtime/no-runtime DMG 与 SHA-256。
- 发布不操作用户 Prefix、游戏目录、账户或配置。发生 App 回归时可重新安装上一版 App，保留原容器；新增库数据字段由兼容解码处理，真实卸载操作仍须用户确认。
