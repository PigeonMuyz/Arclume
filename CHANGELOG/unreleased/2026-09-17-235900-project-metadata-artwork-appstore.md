# 项目资料匹配：Steam 标志与 App Store 查询

- 修复采用 Steam 资料时未同步透明游戏标志；验证独立标志与 library hero 图片后填入草稿，保存后生效。
- GameDB 中有明确 Steam 映射时，同样补全标志；图片缺失或请求失败不会清空已有自定义图片。
- 新增 App Store 名称、ID、链接查询，区分 Mac 与 iPhone/iPad，默认按应用语言选择中国大陆或美国商店，可手动切换地区。
- App Store 结果先预览再采用，应用图标不冒充游戏标志；仅更新展示资料，不改变程序路径、Steam 启动 ID、容器或实际运行平台。
- 使用短期缓存与请求间隔；未接入账户游戏列表。无需 API 密钥，不发送安装路径。

## 验证与兼容性

- 新增 URL、解码、缺失图片保留等隔离测试；继续验证展示资料不会改变启动身份。
- 手工验证：编辑项目 → 匹配资料 → Steam 查询 413150，检查标志预览、采用和保存后的启动器；App Store 查询名称或链接，检查地区/平台与取消不保存。
- 现有展示资料 schema 兼容，无迁移，不修改 Runtime 或用户容器；可重新编辑图片或回退代码。
- 弹窗新布局仅在 Sketch 中提供对比方案，本次不替换未确认的整体布局。
