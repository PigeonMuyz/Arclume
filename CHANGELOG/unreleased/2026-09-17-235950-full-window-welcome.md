# 全窗口欢迎引导

- 首次欢迎由小弹窗改为铺满主窗口的独立界面，保留系统窗口控制，不强制进入 macOS 独占全屏。
- 使用提供的图标背景和主体作为独立资源，加入轻微浮动动画；快速指引以动态箭头说明操作、搜索筛选、设置入口。
- 支持上一步、跳过、回车继续和减少动态效果；Steam 配置保持可选，完成后才记录引导状态。
- 设置的通用页可重新查看使用引导，不重置已经保存的配置。
- 引导期间隐藏库工具栏并禁用底层点击，不改变游戏、容器、Runtime 和版本号。新增隔离欢迎页预览场景。
