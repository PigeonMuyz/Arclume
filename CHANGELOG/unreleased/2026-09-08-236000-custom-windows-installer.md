# 自定义 Windows 安装包入口

- 游戏库“添加游戏”菜单区分“安装 Windows 程序”和“添加已安装游戏”。
- 支持本地 EXE / MSI，选择 Arclume Wine 或 CrossOver 及其现有容器；验证安装包基本格式，按参数数组启动，保留原始安装文件。
- 异步准备期间禁用重复点击，展示启动失败；启动成功不冒充安装完成，不把安装包保存为游戏入口。
- 不改变当前游戏库容器、Runtime 版本或已有 Steam 修补；不创建、迁移、清理用户容器。
- 验证：B0 build、预检；不运行 XCTest。实际第三方安装向导及启动器兼容性需用户手工验证。
