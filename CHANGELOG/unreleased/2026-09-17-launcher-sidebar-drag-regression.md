# 修复启动器侧边栏拖动排序和展开动画

- 侧边栏内部排序改用本地拖动手势，不再经过系统跨应用数据传输；仅允许拖动已有游戏，不接受外部内容。
- 插入白线和落点排序使用同一位置计算，显示随鼠标移动的图标预览；放下或离开列表后清除指示，列表外松手取消排序。
- 侧边栏保留稳定视图，通过位移与透明度实现滑入、滑出；收起后不接受点击，不出现在辅助功能导航中。减少动态效果开启时仅淡入淡出。
- 保留已有图标尺寸、悬停效果和背景处理。不修改游戏、容器或 Runtime，也不变更版本号。
