# YY 自身离屏绘制 CPU 路径对照

- 增加独立 `--run-osr-cpu` 诊断模式，保留 DNS 补丁，撤去额外 Chromium GPU/WebGL 参数。
- 只在两个精确路径与 SHA-256 匹配的 YY 组件加载事件中，将自身配置写入 `cef_osr_gpu` 的 `1` 参数替换为既有 `0`，走原有赋值与清理流程。
- 增加指令/字符串核对及私有内存自检，不改磁盘 DLL、注册表、公共 Runtime 或用户绿色适配数据；退出进程回滚。
- 仅实验工具，频道显示、交互、音频及无调试器运行尚需验证，不进入正式设置或发布包。
