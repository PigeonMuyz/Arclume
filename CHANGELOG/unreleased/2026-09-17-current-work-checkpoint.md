# 保存统一容器与启动器界面现有工作

- 按维护者要求，将当前未提交的应用、资源、测试和文档改动保存为本地基线；各功能细节参见本目录此前的独立记录。
- 包括统一容器升级、YY 启动支持、预热与退出清理、安装发现、启动器界面和侧边栏拖动修复。
- 保留现有 1.0.4 (9) 版本及更新兼容代码。本提交不是发布指令，不推送、不触发 Release。
- 交付前运行 pr_preflight.sh（Debug 构建、Manifest/归档 SHA、发布 YAML 与差异检查）。真实迁移和游戏运行不作为这次快照提交的新验收；不再次迁移或修改本机用户容器。
- 这是后续“全部游戏”入口与单一启动器界面之前的源码回退点；回退源码不意味着能够回退已经迁移的用户数据。
