# 升级引导与副本清理

- 引导改称“升级 Arclume”，精简完成页并移除成功后的副本、恢复入口。
- macOS 26 及以上按钮使用系统 glass / glassProminent，旧系统保留标准按钮。
- 校验并切换成功后自动清理本事务的旧容器与偏好快照；迁移失败仍可恢复，清理中断只能继续清理。
- 兼容已完成但仍留副本的旧事务；清理前更新容器内 Steam 库书签，保留外部库和兼容路径链接。
- 不改 Runtime、版本号或发布；新增隔离测试覆盖清理中断、书签、边界和拒绝回退。成功清理后不支持旧副本恢复。
