# 详情页统一滚动与海报修正

- 海报改为铺满宽度的顶部横幅，移除模糊补边；点击可查看不裁切的完整图片。
- 海报、标题、操作与信息使用一个滚动区域，关闭栏独立固定，避免详情被挤在底部。
- 信息采用原生 GroupBox、LabeledContent、Picker 和按钮，不嵌套滚动 Form。
- 不修改 Dock 图标、Runtime 或用户数据。构建后覆盖 Applications 验收。
