# 2026-08-27 — 修正内置 Wine 的 D3DMetal 模块加载布局

- 启动前将 D3DMetal 4 的 Windows/Unix 模块原子写入内置 Wine 的实际模块目录，使 `d3d11.dll` 和 `dxgi.dll` 不再回退到 `wined3d`。
- 显式传递 D3DMetal 的 `libd3dshared.dylib` 路径，并在资源不完整时在启动前报错。
- 不修改既有 Games 容器、前缀、注册表或游戏文件。
